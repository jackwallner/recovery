# Only en-US is populated in fastlane/metadata/ so far. `deliver` creates a
# localization for every language listed here, so listing the full 50 would push
# empty listings into ASC. Add locales here as their metadata lands.
languages([
  "en-US"
])

# Draft version is set via Fastfile from ASC_APP_VERSION (see asc-ensure-draft-version).

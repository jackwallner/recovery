# Xcode
build/
DerivedData/
*.xcodeproj
*.xcworkspace
!*.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.xcarchive

# Swift Package Manager
.build/
.swiftpm/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
fastlane/metadata.bak.*
# `deliver download_metadata` pulls the App Review contact's name, email, and
# phone number. This repo is public because GitHub Pages serves the marketing
# site from it, so those files must never be committed.
fastlane/metadata/review_information/
Screenshots/raw/

# Secrets
.env*
*.p8
.asc-state.json

# macOS
.DS_Store

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>Recharge Watch</string>
    <key>CFBundleDisplayName</key>
    <string>Recharge</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <!-- The Watch app never calls HealthKit: the phone owns the model and the
         wrist only reads the snapshot it sends. The entitlement and the
         self-care background mode are gone for that reason. The share string
         stays because `HealthKitService` is compiled into every target out of
         `Shared`, so the binary still references the framework and App Store
         validation wants a purpose string for anything it can see. There is no
         update string: `requestAuthorization` passes `toShare: []`, so a write
         purpose string describes a permission the app can never ask for. -->
    <key>NSHealthShareUsageDescription</key>
    <string>Recharge reads your workouts and heart data from Apple Health to show how long your recovery estimate has left. Recharge never writes Health data.</string>
    <key>WKApplication</key>
    <true/>
    <key>WKCompanionAppBundleIdentifier</key>
    <string>com.jackwallner.recovery</string>
</dict>
</plist>
